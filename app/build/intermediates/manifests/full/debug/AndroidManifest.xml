<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.carlosrolindez.navigatorenh"
    android:versionCode="2"
    android:versionName="Navitron" >

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="25" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="es.carlosrolindez.navigatorenh.SearchActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.default_searchable"
                android:value=".SearchActivity" />

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name="es.carlosrolindez.navigatorenh.InfoActivity"
            android:label="@string/app_name"
            android:parentActivityName="es.carlosrolindez.navigatorenh.SearchActivity" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="es.carlosrolindez.navigatorenh.SearchActivity" />
        android:uiOptions="splitActionBarWhenNarrow"
        </activity>
        <activity
            android:name="es.carlosrolindez.navigatorenh.InOutActivity"
            android:label="@string/app_name"
            android:parentActivityName="es.carlosrolindez.navigatorenh.SearchActivity" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="es.carlosrolindez.navigatorenh.SearchActivity" />
        </activity>
        <activity
            android:name="es.carlosrolindez.navigatorenh.DocActivity"
            android:label="@string/app_name"
            android:parentActivityName="es.carlosrolindez.navigatorenh.SearchActivity" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="es.carlosrolindez.navigatorenh.SearchActivity" />
        </activity>
        <activity
            android:name="es.carlosrolindez.navigatorenh.SettingsActivity"
            android:label="@string/action_settings"
            android:parentActivityName="es.carlosrolindez.navigatorenh.SearchActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="es.carlosrolindez.navigatorenh.SearchActivity" />
        </activity>
    </application>

</manifest>